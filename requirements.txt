flask==3.1.3
requests==2.34.2
dnslib==0.9.26
# optional (SPEC addendum B3): HTTPS certificates for Lens, the phone camera viewer.
# Home SOC imports, starts and runs normally without it, and nothing outside Lens
# notices. Inside Lens, everything that touches a certificate needs it - `lens cert`,
# `run --tls`, `lens pair` and the /lens/pair page - so without it a phone cannot be
# paired at all. They all say so. Install with either of:
#   python -m pip install cryptography
#   python -m pip install .[lens]
# cryptography>=42
# dev
pytest==8.*
